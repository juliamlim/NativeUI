import { Platform, StyleSheet } from 'react-native';
import _ from 'lodash';

const vars = require('./_variables');
const colors = require("./_colors");

const utility = StyleSheet.create();

utility.m = _.mapValues()

module.exports = utility;